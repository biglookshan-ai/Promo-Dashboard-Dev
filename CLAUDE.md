@AGENTS.md

> 本项目的规则、关键点、坑和禁令**全部在 `AGENTS.md`**(所有 AI 工具共读的唯一真源),上面那行会把它导入进来。
> 全局约定(目录结构、正式店禁令等)在 `~/Vibe Coding Dev/AGENTS.md`,Claude Code 通过 `~/.claude/CLAUDE.md`(软链)自动加载。
> 这里只放 **Claude Code 专属**的补充;目前没有。
